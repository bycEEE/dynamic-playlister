<% if @tag %>
  <h1>
    <%= "Playlists tagged with: #{@tag}" %>
  </h1>
<% else %>
  <h1>Playlists:</h1>
  <%= render 'tag_cloud' %>
<% end %>

<% @playlists.each do |playlist| %>
  <%= link_to "#{playlist.name}", playlist %><br>
<% end %>